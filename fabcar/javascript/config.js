const intervalCnt = 160;
const intervalMs = 1000;
const txCnt = 1000000;
const fixedCnt = 2000;

module.exports = {
    txCnt: txCnt,
    intervalCnt: intervalCnt,
    intervalMs: intervalMs,
    fixedCnt: fixedCnt,
}